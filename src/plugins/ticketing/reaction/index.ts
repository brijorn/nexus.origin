import { Client, MessageReaction, User } from "discord.js";
import { FetchedPanel, GuildSettings } from "typings/origin";

export default async (bot: Client, guild: GuildSettings, reaction: MessageReaction, user: User, panel: FetchedPanel) => {

}