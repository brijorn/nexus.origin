import { Client, MessageReaction, User } from "discord.js";
import { GuildSettings, Panel } from "../../../typings/origin";

export default async (bot: Client, guild: GuildSettings, reaction: MessageReaction, user: User, panel: Panel) => {

}